name: flutter_search_bloc
description: A new Flutter project.

version: 1.0.0+1

environment:
  sdk: ">=2.5.0<3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^0.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  pedantic: ^1.8.0+1

flutter:
  uses-material-design: true
